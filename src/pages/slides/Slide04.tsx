import { SlideShowContainer } from '../../components/SlideShowContainer'
import type { SlideProps } from '../../types'

export const Slide04 = (props: SlideProps) => {
  return (
    <SlideShowContainer {...props}>
      <h1>Examples</h1>
    </SlideShowContainer>
  )
}
